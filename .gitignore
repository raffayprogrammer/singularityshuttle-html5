# Source files — kept locally only, not committed
# All source content lives in source/ and is reproduced into pages/ as needed
source/

# Tools (downloaded, not source)
tools/ruffle/
tools/jpexs/
tools/jre-extract/
tools/extracted/
tools/*.exe
tools/*.zip

# npm dependencies (Lighthouse + Playwright — installed locally for dev)
node_modules/
package.json
package-lock.json

# Handover bundles (artifacts, regeneratable from tools/build_handover.py)
handover/

# Adobe Animate working files (binary, not for git)
*.fla.bak
*.xfl/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.vscode/
.idea/

# Build intermediates (uncomment if Animate publishes here too)
# build/pages/*.fla
